pluginManagement {
    repositories {
        maven {
            url 'https://cache-redirector.jetbrains.com/plugins.gradle.org'
        }
    }
}

rootProject.name = 'window-layout-manager'
includeBuild '..'

